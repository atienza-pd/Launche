<Window
    x:Class="UI.Features.LaunchWith.LaunchWithWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:UI.Features.LaunchWith"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:namespace="clr-namespace:UI.Features.LaunchWith"
    d:DataContext="{d:DesignInstance Type=namespace:LaunchWithWindow}"
    Title="Launch With"
    Width="350"
    Height="400"
    mc:Ignorable="d" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid Margin="10,5,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding SelectedProject.Name}" ContentStringFormat="Project: {0}"></Label>
        <ListView
            Grid.Row="1"
            ItemsSource="{Binding DevApps, UpdateSourceTrigger=PropertyChanged}"
            Loaded="ListView_Loaded"
            SizeChanged="ListView_SizeChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Header="Name">
                        <GridViewColumn.HeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="Padding" Value="5,0,0,0" />
                            </Style>
                        </GridViewColumn.HeaderContainerStyle>

                    </GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    HorizontalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding DataContext.LaunchProjectDevAppCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">
                                    <Button.Content>
                                        <Image
                                            Grid.Row="7"
                                            Width="16"
                                            Height="16"
                                            Source="/Resources/rocket-solid-full.png"
                                            ToolTip="Launch" />
                                    </Button.Content>
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
